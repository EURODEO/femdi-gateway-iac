# femdi-gateway-iac

## NOTE

Do not merge this branch as this just exists to test things with Terraform. If you need to run Terraform with this branch you need to change the the project id to something else. Also the vault namespace should be changed to something unique. vault-init.sh also needs modification in pod names to reflect the same name as the namespace name.